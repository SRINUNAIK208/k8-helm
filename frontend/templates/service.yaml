apiVersion: v1 
kind: Service 
metadata: 
  namespace: roboshop 
  name: frontend 
  labels:
     project: roboshop 
     component: frontend
     env: dev 
     tier: frontend
spec:
  selector:
    project: roboshop 
    component: frontend
    env: dev 
    tier: frontend
  ports:
  - protocol: TCP 
    port: 80
    targetPort: 80

